# MS-IME `StyleList\Custom` バイナリ解析

## 対象と目的

- 対象: `undotfiles/ime/key-settings.reg`
- 対象レジストリ: `HKCU\Software\Microsoft\IME\15.0\IMEJP\StyleList\Custom`
- 目的: キー設定スナップショットの内部形式を復元し、アクションコードを逆解析するための前提を記録する

この文書は、保存済みスナップショットから確認できる事実と、今後の実験で検証する事項を分けて記録する。スナップショットの更新手順やレジストリへの適用手順は `undotfiles/ime/SPEC.md` の対象である。

## 結論

`key-settings.reg` は暗号化・圧縮された未知のバイナリではない。次の層を順に復元すると、キー設定レコードを人間が読める形までデコードできる。

1. `.reg` ファイル全体を UTF-16LE として読む
2. `hex:` のカンマ区切り値をバイト列へ戻す
3. `REG_BINARY` のバイト列を CP932 として読む
4. NUL区切りのレコードへ分割する
5. 各レコードを最初の `=` でキー名と6個の状態別アクションへ分ける

通常のレコード形式は次のとおりである。

```text
割り当てキー=状態1 状態2 状態3 状態4 状態5 状態6\0
```

右側の6項目は状態別のアクションコードである。各項目は実際のバイナリ1バイトではなく、2桁の16進数を表すASCII文字列である。一部の旧プロファイルには`F3=80`や`L2F1=B0`のような1項目だけのレコードもある。

## エンコードの各層

### `.reg` 全体

ファイル先頭は UTF-16LE BOM (`FF FE`) 付きで、`Windows Registry Editor Version 5.00` から始まる。`file` コマンドでも Windows Registry little-endian text と判定される。

`hex:` 値の行末にある `\` は、`.reg` の論理行が次の物理行へ続くことを示す。

### `hex:` 値の復元

`key` 値の先頭は次のバイト列である。

```text
53,50,41,43,45,3d,38,31,20,30,36,20,32,44,20,33,30,20,30,37,20,30,36,00
```

カンマ区切りをバイト列へ戻すと、CP932/ASCII上では次のレコードになる。

```text
SPACE=81 06 2D 30 07 06\0
```

外側の`3d`はASCIIの`=`、`32,44`はASCII文字列の`2D`に対応する。

### CP932

日本語の割り当てキーは CP932 で格納されている。

| バイト列の例 | 復元文字列 |
|---|---|
| `94 bc 8a 70 2f 91 53 8a 70` | `半角/全角` |
| `83 4a 83 5e 83 4a 83 69` | `カタカナ` |
| `82 d0 82 e7 82 aa 82 c8` | `ひらがな` |

英数字、`+`、空白、`=`、アクションコードはASCIIである。

### NUL区切り

各レコードはNUL (`00`) で終わる。値の末尾にはレコード終端に加えて最終NULがあり、バイト列上はNULが2つ続く。

キー名に空白や`+`が含まれるため、空白では分割しない。最初に現れる`=`だけを区切りとして使う。

## 6状態のアクション配列

右側の6項目は、次の順序で状態に対応する。

| 位置 | IME状態 |
|---:|---|
| 1 | 入力変換済み文字なし |
| 2 | 入力文字のみ |
| 3 | 変換済み |
| 4 | 候補一覧表示中 |
| 5 | 文節長変更中 |
| 6 | 変換済み文節内入力文字 |

したがって、次のレコードは半角/全角キーに対して全状態で同じアクションを割り当てる。

```text
半角/全角=CD CD CD CD CD CD
```

次のレコードは状態ごとに異なるアクションを割り当てる。

```text
Ctrl+BackSpace=84 55 1F 1F 00 15
```

左側が割り当てキー、右側が6状態それぞれのアクションである。`80`、`FF`、`FE`、`00`などのアクションコード自体の意味は、別途検証が必要である。

## ライブレジストリのデフォルトプロファイル

Windows上のライブレジストリを、次の基底パスから読み取った。

```text
HKCU\Software\Microsoft\IME\15.0\IMEJP\StyleList
```

`Custom`以外には、次の5プロファイルが存在した。括弧内は各REG_BINARY値のレコード数である。`Color`配下の表示色バイナリはキー設定レコードに含めていない。

| プロファイル | キー設定フィールド |
|---|---|
| `ATOK` | `key`(88), `S1key`(1), `S3key`(1), `S4key`(3), `S6key`(14), `S7key`(11), `S8key`(1), `S9key`(5), `SDkey`(9) |
| `MS-IME2000` | `key`(62), `S1key`(1), `S3key`(1), `S4key`(6), `S5key`(7), `S7key`(12), `S8key`(1), `S9key`(3), `SDkey`(9) |
| `NATURAL` | `key`(65), `S1key`(1), `S3key`(1), `S4key`(6), `S5key`(7), `S7key`(12), `S8key`(10), `S9key`(3), `SAkey`(2), `SBkey`(1), `SCkey`(1), `SDkey`(9), `SEkey`(2) |
| `VJE` | `key`(85), `S1key`(1), `S3key`(2), `S4key`(2), `S7key`(12), `S8key`(1), `S9key`(3), `SDkey`(8) |
| `WX` | `key`(87), `S1key`(1), `S3key`(2), `S4key`(3), `S7key`(10), `S8key`(1), `S9key`(3), `SDkey`(8) |

プロファイルごとの補助値は次のとおりである。

- `ATOK`, `VJE`, `WX`: `defRoma=MS-IME`, `DisableFunctions=3`
- `MS-IME2000`: `DisplayName=IME Standard`, `defRoma=MS-IME`, `DisableFunctions=2`
- `NATURAL`: `DisplayName=Microsoft IME`, `defRoma=MS-IME`, `DisableFunctions=0`
- `Custom`: `DisableFunctions=0`, `Color\RoamVal=1`

現在の `HKCU\Software\Microsoft\IME\15.0\IMEJP\MSIME` は `keystyle=Custom`、`style=NATURAL` である。`key-settings.reg` はライブの`StyleList\Custom`をexportした内容とバイト単位で一致する。

## 未解明アクション一覧

`key-settings.reg`の`StyleList\Custom`と、標準のMicrosoft IMEプロファイル（`StyleList\NATURAL`）から観測できる全アクションコードを、1コード1行へ整理する。既存の候補コードも行として残し、観測箇所がないプロファイル側は`—`とする。

状態番号はバイナリ配列上の位置である。状態1〜6は、それぞれ「入力変換済み文字なし」「入力文字のみ」「変換済み」「候補一覧表示中」「文節長変更中」「変換済み文節内入力文字」を表す。表では状態番号だけを単独で置かず、`KeyA(1,2,3); KeyB(4)`の形式でキー名と組にする。

`Customの観測箇所`は`key-settings.reg`から生成した。`Microsoft IMEの観測箇所`は、`reg.exe export "HKCU\Software\Microsoft\IME\15.0\IMEJP\StyleList\NATURAL" "%TEMP%\msime-natural.reg" /y`で一時ファイルへexportし、`key`および`S*key`の120レコードから抽出した。`Color`配下は対象外とする。

照合時は`Custom`をbaselineにしてGUIで1つのキー設定だけを変更し、再export後に変化したコードと状態位置を確認する。GUI表示名を4列目、キー名・対象状態・export差分などの根拠を5列目へ記録する。

2026-09-20にcustomとnatural両プロファイルの「キーと操作の対応の変更」画面を撮影し、転記を`~/.pi/agent/work/dotfiles/20260920-135223-ime-custom-profile-gui.research.md`（custom、PNG 10枚）と`~/.pi/agent/work/dotfiles/20260920-135900-ime-natural-profile-gui.research.md`（natural、PNG 10枚）へ記録した。この転記とexportレコードを機械的に突合し、4列目と5列目を次の方針で記入した。

- GUIの列順とexportの状態1〜6の対応は未校正である。記入した名称は、キー行内の列の並びが状態順と一致するという前提に依存する暫定値である
- 完全に読めるセルだけで名称を記入する。右端切れ・キー名切れ・セル重なりのコードは名称を空欄にし、観測内容を確認メモへ記録する。割当なし（`-`）表示はアクション名として扱わず確認メモへ記録し、実際の名称が複数あるコードだけ`/`区切りで併記する
- 同一コードでもキー・状態・プロファイルにより表示が異なる（`14`、`15`、`18`など）。GUI表示はコード単位で一意とは限らない
- 両プロファイルとも`Ctrl+P`と`Ctrl+S`の間の行は撮影範囲外である

| コード | Customの観測箇所 | Microsoft IMEの観測箇所 | GUI上のアクション名 | 確認メモ |
|---|---|---|---|---|
| `00` | Ctrl+ImeOn(2,3,4,5,6) | 半角/全角(2,3,4,5,6) |  | GUI観測: 該当セルは割当なし表示のみ |
| `04` | — | — |  | 観測箇所がないためGUI照合できず |
| `06` | SPACE(2,6) | SPACE(2,6) | 全変換（暫定） | GUI観測: SPACEの状態2,6 |
| `07` | SPACE(5) | SPACE(5) | 文節変換（暫定） | GUI観測: SPACEの状態5 |
| `09` | Ctrl+Enter(2,3,4,5,6) | Ctrl+Enter(2,3,4,5,6) | 全確定（暫定） | GUI観測: Ctrl+Enterの状態2〜6 |
| `0A` | Ctrl+↓(3,4,5,6) | Ctrl+↓ Ctrl+N(3,4,5,6) | 文節確定（暫定） | GUI観測: Ctrl+↓の状態3〜6。NATURALはCtrl+N |
| `0B` | Shift+↓(3,5,6) | Shift+↓(3,5,6) | 先頭確定（暫定） | GUI観測: Shift+↓の状態3,5,6 |
| `0D` | — | — |  | 観測箇所がないためGUI照合できず |
| `0E` | Shift+Esc(3,4,5,6) | Shift+Esc(3,4,5,6) | 全戻し（暫定） | GUI観測: Shift+Escの状態3〜6 |
| `0F` | Esc(5) | Esc(5) | 文節戻し（暫定） | GUI観測: Escの状態5 |
| `11` | ←(2,3,4,6) | ← Ctrl+S(2,3,4,5,6) | 文字左/文節左（暫定） | GUI観測: ←の状態2,6=文字左、NATURALの状態5=文節左。状態3,4は`文節左or文…`で切れ |
| `12` | →(2,3,4,6) | → Ctrl+D(2,3,4,5,6) | 文字右/文節右（暫定） | GUI観測: →の状態2,6=文字右、状態3,4=文節右。NATURALは→ Ctrl+D |
| `13` | Ctrl+←(2,6) | Ctrl+← Ctrl+A(2,6) | 文字先頭（暫定） | GUI観測: Ctrl+←の状態2,6。NATURALはCtrl+A |
| `14` | Ctrl+→(2,6) | Enter(2,6) | 文字末尾/全確定（暫定） | GUI観測: Custom Ctrl+→の状態2,6=文字末尾、NATURAL Enterの状態2,6=全確定で対立 |
| `15` | BackSpace(2,3,4,6) | Shift+BackSpace(2,3,4,6) | 前字削除/候補閉（暫定） | GUI観測: Custom BackSpace、NATURAL Shift+BackSpace。状態2,3,6=前字削除、状態4=候補閉 |
| `16` | Delete(2,3,4,6) | Delete Ctrl+G(2,3,6) | 1文字削除/候補閉（暫定） | GUI観測: Custom Delete、NATURAL Delete Ctrl+G。状態2,3,6=1文字削除、状態4=候補閉 |
| `17` | Ctrl+←(3,4,5) | Ctrl+← Ctrl+A(3,4,5) | 文節先頭/先頭候補（暫定） | GUI観測: Ctrl+←の状態3=文節先頭、状態4=先頭候補。コード表上の状態5は割当なし表示 |
| `18` | →(3,4,5) | Ctrl+→ Ctrl+F(3,4,5) | 文節右/文節末尾/最後候補（暫定） | GUI観測: Custom →の状態3〜5=文節右。NATURAL Ctrl+→は状態3=文節末尾、状態4=最後候補、状態5=割当なしと対立 |
| `19` | Home(3) | Ctrl+← Ctrl+A(3) | 文節先頭（暫定） | GUI観測: Homeの状態3。NATURALはCtrl+←・Ctrl+A |
| `1A` | End(3) | Ctrl+→ Ctrl+F(3) | 文節末尾（暫定） | GUI観測: Endの状態3。NATURALはCtrl+→・Ctrl+F |
| `1B` | ↓(2,4,6) | ↓ Ctrl+X(2,4,6) | 次候補（暫定） | GUI観測: ↓・Ctrl+Xの状態4=次候補、状態2,6=割当なし |
| `1C` | Shift+SPACE(2,3,4,6) | Shift+SPACE(2,3,4,6) |  | GUI観測: Shift+SPACEの状態2〜4,6（全変換・前候補）。キー名`Shift+SPAC…`が切れるため保留 |
| `1D` | PageDown(2,4,6) | PageDown(2,4,6) | 次候補群（暫定） | GUI観測: PageDownの状態4。状態2,6は割当なし表示 |
| `1E` | PageUp(2,4,6) | PageUp(2,4,6) | 前候補群（暫定） | GUI観測: PageUpの状態4。状態2,6は割当なし表示 |
| `1F` | Ctrl+BackSpace(3,4) | Ctrl+Z(3,4) | 文節戻し（暫定） | GUI観測: Custom Ctrl+BackSpace、NATURAL Ctrl+Zの状態3,4 |
| `20` | Shift+←(3,4,5) | Shift+← Ctrl+K(3,4,5) | 文節長-1（暫定） | GUI観測: Shift+←の状態3〜5。NATURALはCtrl+K |
| `21` | Shift+→(3,4,5) | Shift+→ Ctrl+L(3,4,5) | 文節長+1（暫定） | GUI観測: Shift+→の状態3〜5。NATURALはCtrl+L |
| `22` | — | — |  | 観測箇所がないためGUI照合できず |
| `24` | Ctrl+U(3,4,5,6) | F6 Ctrl+U(3,4,5,6) | ひらがな（暫定） | GUI観測: Custom Ctrl+U、NATURAL F6・Ctrl+Uの状態3〜6 |
| `25` | 変換(3,4,5,6) | F7 Ctrl+I(3,4,5,6) | カタカナ（暫定） | GUI観測: Custom変換、NATURAL F7・Ctrl+Iの状態3〜6 |
| `26` | Shift+ImeOff(3,4,5,6) | F9 Ctrl+P(3,4,5,6) |  | GUI観測: `Shift+ImeO…`の切れたキー名が2行に特定できないため保留（該当表示は全角英数） |
| `27` | Ctrl+O(3,4,5,6) | F8 Ctrl+O(3,4,5,6) | 半角（暫定） | GUI観測: Custom Ctrl+O、NATURAL F8・Ctrl+Oの状態3〜6 |
| `28` | 無変換(2,3,4,5,6) | Ctrl+ImeOff(2,3,4,5,6) | かな変換（暫定） | GUI観測: 無変換の状態2〜6。NATURALはCtrl+ImeOff |
| `29` | Shift+無変換(2,3,4,5,6) | Shift+無変換(2,3,4,5,6) | 英数（暫定） | GUI観測: Shift+無変換の状態2〜6 |
| `2B` | Enter(4) | Enter Ctrl+M(4) | 候補選択+全確定（暫定） | GUI観測: Enterの状態4。NATURALはCtrl+M |
| `2D` | SPACE(3) | SPACE(3) | 拡張候補（暫定） | GUI観測: SPACEの状態3 |
| `2F` | Ctrl+T(3,4,5,6) | F10 Ctrl+T(3,4,5,6) | 半角英数（暫定） | GUI観測: Custom Ctrl+T、NATURAL F10・Ctrl+Tの状態3〜6 |
| `30` | SPACE(4) | SPACE(4) |  | GUI観測: SPACEの状態4は`変換+次…`で切れ |
| `32` | — | — |  | 観測箇所がないためGUI照合できず |
| `35` | — | Enter(4) |  | GUI観測: NATURAL Enterの状態4=候補選択+全確定。セルが列5へ溢れ`2B`と分離できないため保留 |
| `36` | BackSpace(4) | BackSpace Ctrl+H(4) | 候補閉（暫定） | GUI観測: Custom BackSpace、NATURAL Ctrl+Hの状態4 |
| `38` | Home(2,4,6) | Home(2,4,6) | 文字先頭/候補先頭（暫定） | GUI観測: Homeの状態2,6=文字先頭、状態4=候補先頭。状態5は割当なし表示 |
| `39` | End(2,4,6) | End(2,4,6) | 文字末尾/候補最後（暫定） | GUI観測: Endの状態2,6=文字末尾、状態4=候補最後。状態5は割当なし表示 |
| `3E` | Ctrl+→(4) | Ctrl+→ Ctrl+F(4) | 最後候補（暫定） | GUI観測: Ctrl+→・Ctrl+Fの状態4 |
| `3F` | ←(3,4) | ← Ctrl+S(3,4) |  | GUI観測: ←・Ctrl+Sの状態3,4は`文節左or文…`で切れ |
| `40` | Shift+変換(2) | Ctrl+Shift+漢字(2) |  | GUI観測: NATURAL `Ctrl+Shift+…`行はキー名末尾が不明のため保留（状態2） |
| `46` | Shift+SPACE(2) | Shift+SPACE(2) |  | GUI観測: Shift+SPACEの状態2に対応する行のキー名が`Shift+SPAC…`で切れ |
| `49` | — | — |  | 観測箇所がないためGUI照合できず |
| `4C` | Shift+↓(2) | Shift+↓(2) | 1文字確定（暫定） | GUI観測: Shift+↓の状態2 |
| `4D` | Esc(2) | Esc(2) | 全消去（暫定） | GUI観測: Escの状態2 |
| `51` | — | — |  | 観測箇所がないためGUI照合できず |
| `52` | — | — |  | 観測箇所がないためGUI照合できず |
| `53` | Ctrl+↑(2) | Ctrl+↑(2) | 文字先頭（暫定） | GUI観測: Ctrl+↑の状態2 |
| `54` | Ctrl+↓(2) | Ctrl+↓ Ctrl+N(2) | 文字末尾（暫定） | GUI観測: Ctrl+↓・Ctrl+Nの状態2 |
| `55` | Ctrl+BackSpace(2) | Ctrl+BackSpace(2) |  | GUI観測: Ctrl+BackSpaceの状態2（前字削除）。キー名切れのため保留 |
| `62` | Ctrl+Y(2,3,4,5,6) | F5 Ctrl+Y(2,3,4,5,6) |  | GUI観測: Ctrl+Y・F5の状態2〜6は`部首・コード…`で切れ |
| `64` | Ctrl+U(2) | F6 Ctrl+U(2) | ひらがな（暫定） | GUI観測: Custom Ctrl+U、NATURAL F6の状態2 |
| `65` | 変換(2) | F7 Ctrl+I(2) | カタカナ（暫定） | GUI観測: Custom変換、NATURAL F7・Ctrl+Iの状態2 |
| `66` | Shift+ImeOff(2) | F9 Ctrl+P(2) |  | GUI観測: `Shift+ImeO…`の切れたキー名が2行に特定できないため保留（該当表示は全角英数） |
| `67` | Ctrl+O(2) | F8 Ctrl+O(2) | 半角（暫定） | GUI観測: Custom Ctrl+O・F8、NATURAL F8の状態2 |
| `6F` | Ctrl+T(2) | F10 Ctrl+T(2) | 半角英数（暫定） | GUI観測: Custom Ctrl+T、NATURAL F10の状態2 |
| `7D` | Ctrl+←(4) | Ctrl+← Ctrl+A(4) | 先頭候補（暫定） | GUI観測: Ctrl+←・Ctrl+Aの状態4 |
| `80` | 変換(1); TAB(1,3,5,6); ← (2,3,5,6) | ↓ Ctrl+X(1,2,3,6); ← (2,3,5,6) | —（暫定） | GUI観測: 変換の状態1は割当なし表示。TAB・←では文字左・文節左表示と重なりコード単位の名称は確定できない |
| `81` | SPACE(1,2,6) | SPACE(1,2,6) | 空白/全変換（暫定） | GUI観測: SPACEの状態1=空白、状態2,6=全変換 |
| `82` | Ctrl+Shift+SPACE(1,2,3,4,5,6) | Ctrl+Shift+SPACE(1,2,3,4,5,6) |  | GUI観測: `Ctrl+Shift+…`行の全状態が全角空白。キー名切れのため保留 |
| `83` | Ctrl+SPACE(2,3,4,5,6) | Ctrl+SPACE(2,3,4,5,6) | 半角空白（暫定） | GUI観測: Ctrl+SPACEの状態2〜6 |
| `84` | Ctrl+BackSpace(1) | Ctrl+BackSpace(1) |  | GUI観測: Ctrl+BackSpaceの状態1（確定取消）。キー名切れのため保留 |
| `85` | — | — |  | 観測箇所がないためGUI照合できず |
| `87` | Ctrl+ImeOn(1) | 変換(1) | 再変換（暫定） | GUI観測: Custom Ctrl+ImeOn、NATURAL変換の状態1 |
| `88` | Shift+SPACE(1) | Shift+SPACE(1) |  | GUI観測: Shift+SPACEの状態1（別幅空白）。キー名切れのため保留 |
| `89` | AppsKey(4) | Shift+F10(4) | 候補メニュー（暫定） | GUI観測: Custom AppsKey、NATURAL Shift+F10の状態4 |
| `8A` | TAB(2,4) | TAB(2,4) |  | GUI観測: TABの状態2=`予測候補表…`、状態4=`候補表示切…`で切れ |
| `8C` | — | — |  | 観測箇所がないためGUI照合できず |
| `8D` | →(2,4) | → Ctrl+D(4) | 文字右/文節右（暫定） | GUI観測: Custom →の状態2=文字右、状態4=文節右。NATURALは→ Ctrl+D |
| `8E` | ←(2,4) | ← Ctrl+S(4) |  | GUI観測: NATURAL ← Ctrl+Sの状態4は`文節左or文…`で切れ |
| `91` | — | — |  | 観測箇所がないためGUI照合できず |
| `92` | — | — |  | 観測箇所がないためGUI照合できず |
| `93` | — | — |  | 観測箇所がないためGUI照合できず |
| `94` | Alt+ImeOn(1,2,3,4,5,6) | ローマ字(1,2,3,4,5,6) | 入力方式（暫定） | GUI観測: Alt+ImeOn・ローマ字の全状態 |
| `95` | — | — |  | 観測箇所がないためGUI照合できず |
| `97` | 無変換(1) | 無変換(1) | かな切替（暫定） | GUI観測: 無変換の状態1 |
| `98` | Shift+無変換(1) | Shift+無変換(1) | 英数切替（暫定） | GUI観測: Shift+無変換の状態1 |
| `99` | — | — |  | 観測箇所がないためGUI照合できず |
| `9A` | — | — |  | 観測箇所がないためGUI照合できず |
| `9B` | — | — |  | 観測箇所がないためGUI照合できず |
| `9C` | — | — |  | 観測箇所がないためGUI照合できず |
| `A1` | — | — |  | 観測箇所がないためGUI照合できず |
| `A2` | — | — |  | 観測箇所がないためGUI照合できず |
| `A4` | 英数(1,2,3,4,5,6) | 英数(1,2,3,4,5,6) | かな英数（暫定） | GUI観測: 英数の全状態 |
| `A5` | — | — |  | 観測箇所がないためGUI照合できず |
| `A6` | — | — |  | 観測箇所がないためGUI照合できず |
| `AC` | Enter(2) | — | 全確定（暫定） | GUI観測: Custom Enterの状態2 |
| `AD` | F2(3,4,5) | F2(3,4,5) | 文字編集（暫定） | GUI観測: F2の状態3〜5 |
| `B0` | — | — |  | 観測箇所がないためGUI照合できず |
| `B1` | — | — |  | 観測箇所がないためGUI照合できず |
| `B3` | — | 半角/全角(2,3,4,5,6) |  | GUI観測: 該当セルは割当なし表示のみ |
| `B7` | Ctrl+Delete(4) | Ctrl+Delete(4) |  | GUI観測: Ctrl+Deleteの状態4=単語削除。キー名右端切れのため保留 |
| `C1` | — | — |  | 観測箇所がないためGUI照合できず |
| `C3` | ひらがな(1) | ひらがな(1) | ひらがなキー（暫定） | GUI観測: ひらがなの状態1 |
| `C4` | カタカナ(1) | カタカナ(1) | カタカナキー（暫定） | GUI観測: カタカナの状態1 |
| `C5` | F8(1) | F8(1) |  | GUI観測: 該当セルは割当なし表示のみ |
| `C6` | F9(1) | F9(1) |  | GUI観測: 該当セルは割当なし表示のみ |
| `C7` | 英数(1) | 英数(1) | かな英数（暫定） | GUI観測: 英数の状態1 |
| `C9` | カタカナ(1,2,3,4,5,6) | カタカナ(1,2,3,4,5,6) | カタカナキー（暫定） | GUI観測: カタカナの全状態 |
| `CA` | ひらがな(1,2,3,4,5,6) | ひらがな(1,2,3,4,5,6) | ひらがなキー（暫定） | GUI観測: ひらがなの全状態 |
| `CC` | — | — |  | 観測箇所がないためGUI照合できず |
| `CD` | 半角/全角(1,2,3,4,5,6) | 半角/全角(1) | IME-オン/オフ（暫定） | GUI観測: 半角/全角の全状態。表示は`IME-オン/オ…`で切れるが6状態すべて同一 |
| `CE` | カタカナ(1) | 半角/全角(1) |  | GUI観測: NATURAL半角/全角の状態1は`IME-オン/オ…`で切れ。Customカタカナ状態1（カタカナキー）と対立 |
| `CF` | ImeOff(1,2,3,4,5,6) | — | IME-オフ（暫定） | GUI観測: Custom ImeOffの全状態 |
| `D0` | — | — |  | 観測箇所がないためGUI照合できず |
| `D1` | — | — |  | 観測箇所がないためGUI照合できず |
| `D2` | TAB(2,3,4) | TAB(2,3,4) |  | GUI観測: TABの状態2=`予測候補表…`、状態4=`候補表示切…`で切れ。状態3は割当なし表示 |
| `D3` | — | — |  | 観測箇所がないためGUI照合できず |
| `D5` | — | — |  | 観測箇所がないためGUI照合できず |
| `F0` | — | — |  | 観測箇所がないためGUI照合できず |
| `F1` | Ctrl+F7(1) | Ctrl+F7(1) | 単語登録（暫定） | GUI観測: Ctrl+F7の状態1 |
| `F3` | — | — |  | 観測箇所がないためGUI照合できず |
| `F4` | — | — |  | 観測箇所がないためGUI照合できず |
| `F5` | Ctrl+変換(1,2,3,5,6) | Ctrl+変換(1,2,3,5,6) | メニュー（暫定） | GUI観測: Ctrl+変換の状態1〜3,5,6。状態4は読み候補（F7） |
| `F7` | Ctrl+変換(4) | Ctrl+変換(4) | 読み候補（暫定） | GUI観測: Ctrl+変換の状態4 |
| `F8` | Ctrl+F8(2,3,4,5,6) | Ctrl+F8(2,3,4,5,6) | 既定の検索（暫定） | GUI観測: Ctrl+F8の状態2〜6 |
| `F9` | Ctrl+F9(2,3,4,5,6) | Ctrl+F9(2,3,4,5,6) |  | GUI観測: Ctrl+F9の状態2〜6は`選択して検索…`で切れ |
| `FA` | Ctrl+B(2,4) | Ctrl+B(2,4) |  | GUI観測: Ctrl+Bの行が両プロファイルとも未撮影範囲 |
| `FE` | Shift+Enter(2,3,4,5,6) | Shift+TAB(2,3,4,5,6) |  | GUI観測: 該当セルは割当なし表示のみ |
| `FF` | SPACE(1,2,3,4,5,6) | SPACE(1,2,3,4,5,6) | —（暫定） | GUI観測: SPACEの全状態と対応するが、表示は空白・全変換・拡張候補・`変換+次…`・文節変換と状態別で単一名称なし |

`Custom`の現スナップショットでは91コードを観測できる。GUIで確認した`半角/全角`の6状態がすべて`IME-オン/オ…`（切れた同一表示）であることと、`半角/全角=CD CD CD CD CD CD`が一致するため、`CD=IME-オン/オフ`を暫定対応とする。

`変換=80 65 25 25 25 25`とGUIの`変換`行（状態1は`-`、状態2〜6は`カタカナ`）を照合すると、`80`は状態1の`-`、`65`は状態2の`カタカナ`、`25`は状態3〜6の`カタカナ`である可能性がある。これはGUI観測とexportの対応が確認できるまで暫定とする。

## 未解明のキーフィールド名

### レジストリ値名

`key`と`S*key`は、6状態の位置番号そのものではない。各値のレコード自身が6状態すべてのアクションを持つためである。ライブレジストリで確認できた値名とプロファイルは次のとおりである。

| 値名 | 存在するプロファイル |
|---|---|
| `key` | 全プロファイル |
| `S1key`, `S3key`, `S4key`, `S7key`, `S8key`, `S9key`, `SDkey` | 全プロファイル |
| `S5key` | `Custom`, `MS-IME2000`, `NATURAL` |
| `S6key` | `ATOK`, `Custom` |
| `SAkey`, `SBkey`, `SCkey`, `SEkey` | `Custom`, `NATURAL` |
| `S2key` | 存在を確認できず |

`imjpcus.dll`にも`S1key`〜`SEkey`という文字列は含まれるが、各フィールドの画面名や適用条件を示す公開文字列は見つからない。`S`と16進文字の命名規則は未解明である。

### レコード左側のキー名

通常のキー名のほかに、次のような未解明または複合的なキー名がある。

- 末尾に空白を持つ`← `、`→ `。`主に`SAkey`、`SCkey`に現れる
- `← Ctrl+K`、`← Ctrl+S`、`→ Ctrl+D`、`↑ Ctrl+E`、`↓ Ctrl+X`など、矢印キーと別キーを連結した名前
- `PageDown  Ctrl+X`のように空白数が異なる名前
- `Ctrl+Shift+Home Shift+Home`のように、修飾キー列を含む名前
- `L2F1`〜`L2F15`。`WX\key`にのみ現れ、`L2F1=B0`のように6状態形式ではない1トークンレコードを持つ
- `F3=80`、`F4=80`。`MS-IME2000\key`と`NATURAL\key`に現れる旧形式とみられる1トークンレコード

これらは空白で分割せず、レコードの最初の`=`までをキー名全体として扱う必要がある。

## 値のインベントリ

`REG_BINARY` 値は14個、レコードは合計166件である。サイズは`hex:`をバイト列へ復元した後のサイズである。

| 値名 | バイト数 | レコード数 |
|---|---:|---:|
| `key` | 2133 | 82 |
| `S1key` | 25 | 1 |
| `S3key` | 34 | 1 |
| `S4key` | 168 | 6 |
| `S5key` | 257 | 10 |
| `S6key` | 378 | 17 |
| `S7key` | 399 | 16 |
| `S8key` | 241 | 10 |
| `S9key` | 85 | 4 |
| `SAkey` | 58 | 2 |
| `SBkey` | 33 | 1 |
| `SCkey` | 23 | 1 |
| `SDkey` | 327 | 13 |
| `SEkey` | 57 | 2 |
| 合計 | 4218 | 166 |

## DWORD値

バイナリ値以外には次のDWORD値がある。

| レジストリ値 | 値 |
|---|---:|
| `DisableFunctions` | `0x00000000` |
| `Color\RoamVal` | `0x00000001` |

## 現時点で確定していること

- `.reg` 全体は UTF-16LE である
- `hex:` の値を戻すと、CP932のNUL区切りテキストになる
- レコードは `割り当てキー=状態別アクション` 形式である
- 左側は割り当てキー、右側は6状態のアクション配列である
- 日本語キー名はCP932で復元できる
- `key-settings.reg`のCustomレコードは6個の2桁16進値によるアクション配列で構成される
- 旧プロファイルには1項目だけのレガシーレコードも存在する
- `key` はCustomで通常キーを82件持つ
- `key` と`S*key`のレコードは、左側のキー名と右側の状態別アクション配列で構成される
- `key-settings.reg` はライブの`StyleList\Custom`とバイト単位で一致する
- ライブレジストリには`ATOK`、`MS-IME2000`、`NATURAL`、`VJE`、`WX`、`Custom`の6プロファイルがある
- `Custom`以外のプロファイルにも同じアクションコード体系が使われている
- 一部の旧プロファイルには6状態形式ではない1項目レコードがある
- `DisableFunctions` と `Color\RoamVal` は通常のDWORDである

## 未解明のこと

- 観測された各アクションコードと実際のIMEコマンドの対応
- `80`、`FF`、`FE`、`00`などの特殊値が「無効」「継承」「何もしない」のどれに当たるか。`00`はGUIで割当なし表示と相関する
- GUIの列順とexport状態位置の対応。GUI変更→再export差分の校正で確定する
- 同一コードのGUI表示がキー・状態・プロファイルで異なる事例（`14`、`18`など）の仕組み
- `key` と`S1key`〜`SEkey`の適用順序と、各`S*key`の意味
- `S2key`が存在しない理由
- `← `や`→ `の末尾空白、複合キー名、`L2F1`〜`L2F15`の命名規則
- 旧形式の1項目レコードが表す状態と、6状態配列との関係

## 既存実装との関係

`undotfiles/ime/key-settings.ts` には、今回の外側の形式を扱う処理がすでにある。

- `:14-70`: UTF-16LE `.reg` のセクション、継続行、`hex:`、`dword:`を解析
- `:97-113`: CP932でデコードし、NUL区切りと最初の`=`でレコードを分解
- `:118-136`: レコード単位の追加・削除・変更を比較

現在の実装はCustomの6項目を意味のある状態として解釈せず、17文字のペイロード文字列として比較する。

## 次の逆解析手順

デフォルトプロファイルとの比較で候補を絞り、Windows上の変更前後exportで意味を検証する。

1. `ATOK`、`MS-IME2000`、`NATURAL`、`VJE`、`WX`、`Custom`の同じキー名を状態位置ごとに比較する
2. 既知の操作名から、`BackSpace`、`Delete`、矢印、`Enter`などのアクションコードを暫定対応付けする
3. 現在の`StyleList\Custom`をbaselineとしてexportする
4. MS-IMEのキー設定GUIで、1キーの割り当てだけ変更する
5. 同じキーを再度exportし、変更されたプロファイル値・レコード・状態位置を比較する
6. 入力中、変換中、候補一覧中など状態を変えて同じ実験を繰り返す
7. 複合キー名と1項目レコードは、GUI上の設定項目またはDLLの文字列参照と照合する

まず各状態の対応を固定し、その後にアクションコードとIMEコマンドの対応を確定する。単一の変更で複数状態のコードが変わる場合は、GUIが複数状態へ同時に割り当てた可能性を含めて記録する。
