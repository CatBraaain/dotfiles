sourceDir: dist

diff:
  exclude:
    - scripts
  command: bun
  args:
    - scripts/chezmoi-diff-collate.ts
    - "{{ .Destination }}"
    - "{{ .Target }}"
    - chezmoi-collate.jsonl

persistentState: ~/.config/chezmoi/chezmoi.boltdb
