# Bundle patch layer for dotfiles-dsh-tickets.
# Joined into the profile when this package is listed in dsh.profile.bundles
# (see profiles/web/package.json, managed statically).
- insert:
    - id: dsh-tickets
      name: dotfiles-dsh-tickets
