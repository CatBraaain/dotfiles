# Bundle patch layer for dotfiles-dsh-session-id-system-prompt.
# Joined into the profile when this package is listed in dsh.profile.bundles.
- insert:
    - id: session-id-system-prompt
      name: dotfiles-dsh-session-id-system-prompt
