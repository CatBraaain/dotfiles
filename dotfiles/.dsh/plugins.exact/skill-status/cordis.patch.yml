# Bundle patch layer for dotfiles-dsh-skill-status.
# Joined into the profile when this package is listed in dsh.profile.bundles.
- insert:
    - id: dsh-skill-status
      name: dotfiles-dsh-skill-status
