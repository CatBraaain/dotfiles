# Bundle patch layer for dotfiles-dsh-footer.
# Joined into the profile when this package is listed in dsh.profile.bundles
# (reconciled automatically by `dsh plugin --profile web add`).
- insert:
    - id: dsh-footer
      name: dotfiles-dsh-footer
