# Bundle patch layer for dotfiles-dsh-session-list.
# Joined into the profile when this package is listed in dsh.profile.bundles.
- insert:
    - id: session-list
      name: dotfiles-dsh-session-list
