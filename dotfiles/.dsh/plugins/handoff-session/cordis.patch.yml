# Bundle patch layer for dotfiles-handoff-session.
# Joined into the profile when this package is listed in dsh.profile.bundles.
- insert:
    - id: handoff-session
      name: dotfiles-handoff-session
