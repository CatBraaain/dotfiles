sourceDir: dist

diff:
  exclude:
    - scripts
  command: bun
  args:
    - scripts/chezmoi-diff.ts
    - "{{ .Destination }}"
    - "{{ .Target }}"

persistentState: ~/.config/chezmoi/chezmoi.boltdb
